diff --git a/external/mesa3d/src/glsl/linker.cpp b/external/mesa3d/src/glsl/linker.cpp
index f8b6962..f31e5b5 100644
--- a/external/mesa3d/src/glsl/linker.cpp
+++ b/external/mesa3d/src/glsl/linker.cpp
@@ -67,6 +67,7 @@
 #include <cstdio>
 #include <cstdarg>
 #include <climits>
+#include <stddef.h>
 
 #include <pixelflinger2/pixelflinger2_interface.h>
 
@@ -1762,4 +1763,4 @@ done:
    }
 
    //hieralloc_free(mem_ctx);
-}
\ No newline at end of file
+}
diff --git a/frameworks/base/core/res/res/values/config.xml b/frameworks/base/core/res/res/values/config.xml
old mode 100755
new mode 100644
index a605c93..ab6fb16
--- a/frameworks/base/core/res/res/values/config.xml
+++ b/frameworks/base/core/res/res/values/config.xml
@@ -227,6 +227,11 @@
         <item>4</item>
     </integer-array>
 
+<!-- Regex array of allowable upstream ifaces for tethering - for example if you want
+         tethering on a new interface called "foo2" add <item>"foo\\d"</item> to the array -->
+    <string-array translatable="false" name="config_tether_upstream_regexs">
+    </string-array>
+
     <!-- String containing the apn value for tethering.  May be overriden by secure settings
          TETHER_DUN_APN.  Value is a comma separated series of strings:
          "name,apn,proxy,port,username,password,server,mmsc,mmsproxy,mmsport,mcc,mnc,auth,type"
@@ -752,4 +757,7 @@
     <!-- Use legacy USB device manager. -->
     <bool name="config_usesLegacyUsbDeviceManager">false</bool>
 
+ <!-- Boolean indicating if we require the use of DUN on mobile for tethering -->
+    <bool translatable="false" name="config_tether_dun_required">false</bool>
+
 </resources>
diff --git a/packages/apps/Phone/res/values/config.xml b/packages/apps/Phone/res/values/config.xml
index eb9de4c..d6fa81e 100644
--- a/packages/apps/Phone/res/values/config.xml
+++ b/packages/apps/Phone/res/values/config.xml
@@ -112,4 +112,14 @@
 
     <!-- If true, removes the Voice Privacy option from Call Settings -->
     <bool name="config_voice_privacy_disable">false</bool>
+    <!-- Enable the onscreen touch UI for all states of the in-call UI. -->
+    <bool name="allow_incoming_call_touch_ui">true</bool>
+    <bool name="allow_in_call_touch_ui">true</bool>
+
+    <!-- This is a device with capacitive buttons, so disable the feature
+         where BACK rejects the current incoming call.  (Since it's so
+         easy to press the key accidentally as you pull the phone out of
+         your pocket.) -->
+    <bool name="allow_back_key_to_reject_incoming_call">false</bool>
+
 </resources>
diff --git a/packages/apps/Settings/res/values/bools.xml b/packages/apps/Settings/res/values/bools.xml
index 278859b..934839f 100644
--- a/packages/apps/Settings/res/values/bools.xml
+++ b/packages/apps/Settings/res/values/bools.xml
@@ -19,6 +19,7 @@
     <bool name="has_dock_settings">false</bool>
     <!-- Whether there is a silent mode checkbox  -->
     <bool name="has_silent_mode">true</bool>
+    <bool name="has_intrusive_led">true</bool>
     <!-- Whether the power control widget is enabled for this device. Should be overridden for 
          specific product builds. -->
     <bool name="has_powercontrol_widget">true</bool>
